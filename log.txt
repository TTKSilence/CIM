2019.11.08
    1. 搞清继承关系，尤其是窗口的继承关系（master）
    2. 考虑是否可以用canvas来承载数据库接口来的数据

2019.11.12
    1. '添加'弹窗已完成，输入数据的存储位置/获取方式为
         'user_xxx = self.xxx_entry.get()'

    2. 主页的下拉选项框与用户输入框已完成，用户输入接口已预留

    3. 主程序下的数据处理方法已建立，留有了‘用户输入数据’接口，待后续添加

    4. 剩余工作：
            1. 主页面上搜索结果的显示
            2. 各插件位置的优化，与页面的美化
            3. 数据库方面的所有工作

2019.11.16
    1. 测试完成了数据库的连接、数据表的创建、数据的输入；
    2. 调查如何在代码里创建数据库；
    3. 待完成：数据表中数据的查找与输出；

2019.11.17
    1. 完成数据库部分的各个功能函数
    2. 由于不能交叉import，但是两个.py文件之间需要数据交换，考虑将代码融为一体，解决数据传递和引用的问题

2019.11.21
    1. 完成了主页面上功能按钮的实现问题，通过调用database.search_data实现'查询'功能，并显示出了正确的输出结果；
    2. 掌握了在vscode上用GitHub的pull request功能提交、同步代码的操作；
    3. 下一步，实现将搜索结果输出显示在所设置的label插件上（或者选用更合适的插件）。

2019.11.27
    1. 将search_data部分的位置向外移了一层，试验没有导致bug；
    2. 考虑把放置输出数据的位置放置到插件创建函数内，与各按钮插件并列放置；还是将其单独拎出来放置？（影响因素是 其位置会否跟数据输出的时间先后有关）
    3. 依旧无法解决从函数里将函数内的参数值赋给全局变量的问题；

2019.11.29
    1. 完成了数据库查询结果的输出打印问题，使用的是tk.treeview模块；包括多条记录同时输出；
    2. 对于数据的添加/输入问题，考虑是用弹窗解决，还是依然简化，在treeview中找到添加模块；然后弹窗表示添加结果；(outdated)
    3. 尝试将popup里输入的数据导出，将其导入到database中的input_data函数中，并将执行的结果（success/failure）返回给主窗口/或者弹出弹窗；

2019.12.02
    1. 完成了popup里的数据导出功能：使用wait_window()，将弹窗中输入的数据在其销毁后导出至父窗口（即调用函数的窗口）；注，弹窗函数中不应出现wait_window()，否则会出现bad window报错；
    2. 下一步工作，将popup中传回的数据，通过调用database中的input_data()函数写入数据表中；
    3. 在input_data()执行完之后，给出success/failure状态返回值；